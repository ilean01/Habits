# Estado de trabajo — 23 de septiembre de 2026

Rama: `integrar-biblioteca-privada`. Avance solicitado para guardar y continuar después; no equivale a una migración o despliegue terminado.

## Implementado en esta rama

- Ajustes accesibles en pantallas bajas; barra lateral desplazable y acceso directo al diario.
- Saludo nocturno con luna, capitalización del mes y resumen con hábitos, eventos realizados, lectura y logros, preservando los nombres.
- Selector visual de iconos, atajos de días, campos de cantidad condicionales, hora sugerida, color y orden de fichas arrastrando en notebook.
- Agua por tomas independientes en litros, con corrección individual.
- Fotos privadas de entrenamiento: elegir/tomar desde el dispositivo, ver y eliminar. Requiere la migración del bucket y conexión para subir.
- Calendario semanal, edición o cancelación de una ocurrencia de eventos repetidos.
- Materias, aula, semestre, notas con promedio simple; exámenes/entregas; horarios semanales de trabajo/clases; práctica de inglés por habilidad; tipos de día automáticos.
- Qué sigue, ideas para ratos libres, cierre diario, resumen semanal y logros básicos; consulta manual del clima de Asunción.
- Importación de JSON con revisión, sin sustituir registros existentes.
- Biblioteca avanzada con punto de entrada y compilación separados: catálogo, fichas, filtros, lecturas e historial, préstamos, deseos, favoritos, próximas lecturas, papelera, revisión, duplicados, recomendador básico, ISBN/cámara, datos externos, portadas, QR para cargar desde un celular autorizado, estadísticas, etiquetas PDF, exportación y respaldos.
- Biblioteca compartida mediante miembros explícitos con rol lector/editor. RLS tanto en tablas como en portadas/respaldos. Habits conserva los datos personales separados por cuenta.
- Transacciones PostgreSQL para operaciones relacionadas de lectura y préstamos.
- Infraestructura de notificaciones: registro del dispositivo, service worker, migración y función `send-reminders`. Todavía NO está activada en el servidor.

## Verificación realizada

- 9 pruebas aprobadas: dominio, resumen, recorrido de interfaz demo y PostgreSQL real con aislamiento de cuentas, permisos, revocación, conflictos e idempotencia de Habits, préstamos serializados.
- Compilación Vite de Habits y Biblioteca correcta.
- ZIP original revisado sin modificar: 1.470 libros, 1 lectura, 2 personas, 2 préstamos y 16 configuraciones; 59 portadas locales encontradas y 3 referencias externas.
- Script de migración comprobado en modo de verificación local. Por defecto NO escribe; requiere `--apply` y credenciales administrativas locales. `--resume` conserva registros y portadas existentes.
- Los datos privados, la SQLite y las portadas NO se incluyen en Git.

## Pendiente antes de dar por terminada la entrega

1. Aplicar las migraciones de Biblioteca, miembros, fotos, transacciones y notificaciones en Supabase, en orden. No se ha aplicado ninguna desde esta sesión.
2. Identificar a la persona propietaria y las cuentas que pueden leer o editar. No se concedió acceso a cuentas supuestas. Configurar `biblioteca_access` y `biblioteca_members` administrativamente.
3. Ejecutar y verificar la importación real de SQLite/portadas. Conservar el ZIP y la base original. La clave pública del navegador no permite efectuar esta operación administrativa.
4. Activar notificaciones: generar VAPID, configurar `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `REMINDER_CRON_SECRET` como secretos de Edge Functions; configurar `VITE_VAPID_PUBLIC_KEY` en el build. Desplegar `send-reminders` con verificación JWT desactivada (la función exige su secreto `x-cron-secret`) y programar POST cada minuto con pg_cron/pg_net y secreto en Vault. Falta probar entrega en iPhone y Windows.
5. Verificar SMTP, registro/confirmación y recuperación de cuentas en el proyecto real.
6. Comparar cada flujo de la biblioteca migrada con Flask usando datos reales. El QR actual requiere iniciar sesión con cuenta autorizada; no replica todavía el token temporal de carga de la aplicación original. Recomendador básico por reglas; falta cotejar todas las opciones originales y configuración tipográfica.
7. Completar frecuencia «X veces por semana», feriados paraguayos con traslados verificados, priorización contextual de trabajo y recordatorios por excepciones de serie.
8. Migrar la persistencia a IndexedDB y terminar sincronización incremental: actualmente se conserva el mecanismo original de localStorage y descarga completa. El índice está preparado. Faltan pruebas específicas del cliente offline y conflictos en varias pestañas.
9. Revisar visualmente notebook al 100%, Safari/iPhone instalado y accesibilidad táctil. La prueba automatizada DOM no sustituye esta revisión visual ni la entrega real de push.
10. Fusionar/publicar cuando se complete la revisión. Esta rama no modifica automáticamente GitHub Pages, cuyo workflow publica `main`.

Fuentes de implementación: [Web Push en iOS](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/), [programación de Edge Functions](https://supabase.com/docs/guides/functions/schedule-functions), [Open-Meteo](https://open-meteo.com/en/docs).
