name: Apply HCI consolidation
on:
  push:
    branches: [consolidacion-hci-ingenieria]
permissions:
  contents: write
jobs:
  consolidate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: consolidacion-hci-ingenieria
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: python scripts/apply-consolidation.py
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Commit consolidation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main.js src/enhancements.js
          git diff --cached --quiet && exit 0
          git commit -m "Consolidar navegación, progreso y consistencia HCI"
          git push origin HEAD:consolidacion-hci-ingenieria
